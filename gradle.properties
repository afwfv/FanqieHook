org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# compileSdk 37 exceeds AGP 8.10's recommended max (36); the AAR metadata check
# (libxposed:service 102) only compares the numeric value, so suppress the warning.
android.suppressUnsupportedCompileSdk=37
# android.aapt2FromMavenOverride is intentionally absent for Windows / non-Android-Studio builds.