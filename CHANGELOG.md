# 更新日志

> 发布时，按 module.prop 中的版本号自动读取对应「## x.x.x」段作为模块中心的更新说明；
> 找不到对应版本段时，回退使用「未发布」段。
> 本文件面向使用者：只写「适配了哪个版本 + 用户能感知到的变化」，不写实现细节。

## 分支与发布纪律（务必遵守）

CI 的发布任务**只由 push 到 main 触发**（`.github/workflows/sync-xposed-modules-repo.yml`
里 `on.push.branches: [main]`），推 debug 不会构建、不会发版。

1. **日常改动一律提交到 `debug` 分支**并推 `origin/debug` —— 不碰 main
2. **在真机上验证通过**之后，才把 debug 合入 main
3. 合 main 前：递增 `versionCode` / `version`，并把该版本的说明写进本文件对应段落
4. 合 main → CI 自动构建并发布到自家仓库与模块仓库（tag：`[versionCode]-[versionName]`）
5. 只改文案（README / CHANGELOG）不会触发重新发布——CI 的变更检测只对比 `app/` 目录，
   需要更新发布说明时手动跑一次 `gh workflow run "Sync to Xposed Modules Repo" -f publish_release=true`

反面教训：v0.6.1（未验证）与 v0.7.0（「激励秒领」实测无效）都是没验证就直接推 main
发出去的，用户直接收到了带无效功能的版本。宁可在 debug 上多待几天，也不要发一个
自己没验过的版本。

## 未发布
- 把 73967 加入审计集（`SUPPORTED_VERSION_CODES`），该版本启动时的
  `unverified host version` WARN 变为 INFO。纯日志级别调整，不影响任何 hook。
  73967 的核验依据写在 `FanqieModule` 的审计注释里：静态复核（两个 `ExperimentUtil`
  getter 仍按字段唯一命中、37 个位置名零缺失、实现类仍为 `eg3.a` / `mb3.f`）
  加上真机实测（`installed=29 skipped=1`、`lost=[]`，且记录到 73732 → 73967
  原地升级时字段反查自动由 `q0` 跟到 `s0`）。

## 0.8.4
v0.8.4 - 适配番茄小说 7.3.9.67（versionCode 73967）
- **零广告**：广告位过滤改为一律拦截。`checkAdAvailable` 对所有位置都返回「无广告」，
  被动广告位、用户主动的激励/金币位、以及宿主以后新增的未知位置全部一并拦掉，
  不再需要维护拦截名单
- **代价（重要）**：用户主动点的「看视频得金币 / 看广告免广告 / 看视频催更 / 听书激励 /
  看视频解锁章节」等入口会失效——那里已经无广告可播。发奖仍由服务端校验，
  本地拦广告不会让金币或权益白得；少数「看广告解锁内容」型的位，其表现取决于宿主是
  等广告回调还是乐观放行
- 73967 静态复核：现有 37 个位置名在该宿主里全部存在、零缺失；AB 表 40 个位置已全覆盖
- 73967 实测：install summary `hooks installed=29 skipped=1`、`lost=[]`；
  并记录到一次真实升级（73732 → 73967）中横屏插入广告开关自动由 `q0` 跟到 `s0`
- 拦截日志按来源分类输出 `kind=passive / reward / unnamed`，便于回看拦到了什么

## 0.8.3
v0.8.3 - 适配番茄小说 7.3.9.17（versionCode 73917）
- 解决混淆名跨版本漂移：原本按 `ExperimentUtil#q0()` 硬编码的「横屏插入广告」开关，
  在 73917 上被改名 `s0()Z`（原 `q0` 改成返回 `long` 成了另一个配置），硬编码路径下会静默
  失效。改为按它读取的稳定字段 `landscapeInsertAdEnable` 反查 getter（ClassResolver 用
  DexKit 按「无参 boolean 方法 + 读该字段」查表）；`enableMultiSeriesFlowAd`（系列信息流
  总开关）也换成同样的字段反查。字段名是业务名、跨版本不变，以后宿主再改混淆字母不用重新适配
- 修复状态文件丢诊断的 bug：之前超过上限会把整个文件清成一行 `... truncated ...`，把当次
  启动的「unverified host version」WARN 和每个 hook 的跳过原因都冲掉——恰好是宿主升级后
  定位问题最需要的那几行。改为保留最近 192 KB（按行边界对齐）
- 73917 上 DexKit 自动跟进：广告实现类 `lf3.a` → `eg3.a`，全屏广告 `mb3.f`
- 真机实测 73917：install summary `hooks installed=29 skipped=1`，`lost=[]`（唯一 skip 是
  红果专有类，本机未装红果）

## 0.8.2
v0.8.2 - 适配番茄小说 7.3.7.32（versionCode 73732）、红果免费短剧 7.3.7.32（73732）
- 补拦红果推荐流里的广告位 `recommend_video`（真机实测该位置会走到广告可用性检查但此前
  未分类，属于漏拦）
- 把金币 / 奖励类广告位一次性登记齐全（`gold_coin_reward_*`、`video_gold_coin_reward_*`、
  `listen_coin`、`video_coin_ad` 等），这些是"看广告换金币"的用户主动流程，按设计保留，
  登记后不会再被当成"待分类"反复上报

## 0.8.1
v0.8.1 - 适配番茄小说 7.3.7.32（versionCode 73732）、7.3.7.18（73718）
- **番茄更新后不再需要重新适配**：以前版本号不在支持列表里就一条 hook 都不装，番茄一升级
  模块就变成"装了但完全没效果"，必须等适配。现在改成逐条安装：某条规则的目标没了，只跳过
  那一条并在日志里记下来，其余照常生效——升级最多是少几条效果，不会整体失效
- 新增支持 7.3.7.18（73718）：此前这个版本会被版本校验直接拒绝，模块加载后一个 hook 也不装，
  表现就是"装了但完全没效果"
- 新增两种排查渠道：logcat（`adb logcat -s FanqieHook`）与宿主 cache 目录下的
  `fanqiehook.log` 状态文件。部分 LSPosed 版本不写模块日志、部分设备还关闭了 logcat，
  有状态文件时仍能确认模块是否生效：
  `adb shell su -c 'cat /data/data/com.dragon.read/cache/fanqiehook.log'`
- 安装结果会汇总成一行 `install summary: hooks installed=N skipped=M`，没装上的会列出名字，
  反馈问题时带上这一行就能定位
- 模块名从「番茄 红果 版本7.3.7.32去广告」改为「番茄 红果 去广告」，不再把版本号写进名字

## 0.8.0
v0.8.0 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 红果免费短剧 7.3.7.32（73732）同基线适配
- 激励视频：不提供「秒领」——番茄的发奖由服务端校验，客户端做不到；看满时长正常发奖

## 0.7.1
v0.7.1 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 移除实验性的「激励秒领」：真机实测无效（金币不增加），激励视频恢复为正常观看
- 修复广告关闭回调传空参数会抛异常的问题

## 0.7.0
v0.7.0 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 尝试新增「激励秒领」（默认开启）；该功能经真机实测无效，已在 0.7.1 撤回

## 0.6.3
v0.6.3 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 体积优化：安装包由 1.84 MB 降到 0.47 MB
- 清理定位问题用的临时探针

## 0.6.2
v0.6.2 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 修复番茄开屏 / 全屏广告拦截（此前版本在番茄上一直拦不住，只有红果生效）

## 0.6.1
v0.6.1 - 适配番茄小说 7.3.7.32（versionCode 73732）
- 修正开屏广告的链路判断；该版未彻底解决，0.6.2 已完整修复

## 0.6.0
v0.6.0 - 适配番茄小说 / 红果免费短剧 7.3.7.32（versionCode 73732）
- 扩充广告位拦截名单：新增评论列表、短剧评论、故事插页、创作者广告、短视频进度条、
  横屏短剧插入与暂停、短剧信息流与 banner、听书信息流与贴片等广告位
- 显式保留用户主动的激励视频与金币入口
- 新增「未分类广告位」日志：遇到尚未拦截的广告位会记一条，便于后续适配

## 0.5.0
v0.5.0 - 适配番茄小说 / 红果免费短剧 7.3.7.32（versionCode 73732）
- 新增支持 7.3.7.32，同时保留 7.3.5.32
- 修复版本校验在 Android 14 上失效的问题（读不到宿主版本号时会直接放行）
- 全量复核 26 个拦截目标并逐个比对调用点，确认没有失效的拦截项

## 0.4.0
v0.4.0 - 适配番茄小说 7.3.5.32（versionCode 73532）
- 全量复核 24 个拦截目标类与方法签名，与 73532 逐一比对全部命中
- 唯一缺失的红果专属类在番茄侧预期跳过，不影响功能

## 0.3.0
v0.3.0 - 适配红果免费短剧 + 开屏广告拦截
- 新增红果短剧广告拦截（banner、贴片、横屏插入）与热启动开屏广告阻断
- 作用域覆盖 `com.dragon.read` 与 `com.phoenix.read`
- 番茄侧与红果侧 hook 全部安装成功，红果开屏与短剧 banner / 贴片广告消失
